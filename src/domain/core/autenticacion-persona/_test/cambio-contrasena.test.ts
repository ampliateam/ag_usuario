import * as service from "../service";

describe('CRUD - AutenticacionPersona', () => {
    const timeoutValue = 60000;

    // beforeAll(async () => {
    //     expect(true).toEqual(true);
    // }, timeoutValue);

    test.skip('Algun trabajo', async () => {
        console.log(service.crud);
    }, timeoutValue);
});
